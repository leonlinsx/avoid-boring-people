feedparser
tweepy
python-dotenv
openai
sumy
nltk
numpy
scipy        
pycountry    
requests
atproto
python-frontmatter
setuptools

# The test runner is deliberately not listed here: `requirements.lock` is what
# the publishing workflows install, and a test framework must never be part of
# a publish path. Tests install `requirements-test.lock` instead.
