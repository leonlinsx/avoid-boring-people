"""Lin Scout: the few current external conversations the archive can improve.

Scout is internal distribution tooling. It reads the canonical article index,
queries a small set of external sources, matches candidates against writing that
already exists, judges each survivor, and drafts a possible contribution for the
author to review. It never posts, never messages anyone, and nothing in the
website or newsletter runtime imports it.
"""
